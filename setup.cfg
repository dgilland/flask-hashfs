[pytest]
norecursedirs = env
addopts = --doctest-modules -v -s

# pytest coverage options
[run]
omit =
    */tests/*
    */test_*
    */_compat.py

[pep8]
exclude = .tox,env

[bidst_wheel]
universal = 1
